В начел установка зависимостей
Для запуска: npm run start

Для запуска в режиме production нужно:
0(возможно доскачать npm install -g serve)
1 npm run build - билд
2 serve -s build - запуск билда

Для режима разраба:
npm run start
